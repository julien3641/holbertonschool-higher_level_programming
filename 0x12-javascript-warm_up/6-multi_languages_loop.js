#!/usr/bin/node
// prints 3 lines with an array and a loop

const lines = ['C is fun', 'Python is cool', 'Javascript is amazing'];

for (let i = 0; i < lines.length; i++) {
  console.log(lines[i]);
}
